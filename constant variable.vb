Module Module1
 
    Sub Main()
        'Constant value
        Const pi As Double = 3.145
        Dim radius As Single = 5
        Dim area As Double = pi * radius * radius
        Console.Write("the area of circle is: " & area.ToString)
        Console.ReadLine()
    End Sub
 
End Module
