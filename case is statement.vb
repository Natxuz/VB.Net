Module Module1
    Sub Main()
        'Decision making
        Dim a As Integer = 1
        Select Case a
            Case Is = 1
                Console.WriteLine("Option selected is " & a.ToString)
            Case Is = 2
                Console.WriteLine("Option selected is " & a.ToString)
            Case Is = 3
                Console.WriteLine("Option selected is " & a.ToString)
        End Select
        Console.ReadLine()
    End Sub
End Module
