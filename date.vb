Module Module1
 
    Sub Main()
        'print a date
        Dim today As Date = Now
        Console.Write("the current date is: " & today.ToString)
        Console.ReadLine()
    End Sub
 
End Module
