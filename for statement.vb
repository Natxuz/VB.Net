Module Module1
    Sub Main()
        'Loops
        Dim a, limit As Integer
        limit = 10
        For a = 1 To limit
            Console.WriteLine("Value of a is " & a.ToString)
        Next
        Console.ReadLine()
    End Sub
End Module
